# 更新日志

## 1.0.50 — 2026-09-19

### 修复

- 修复划词翻译中 PDF/API 文本把上标摊平成空格后，`P c`、`M i`、`P 1−N` 不能按公式显示的问题。
- 支持划词翻译返回未包裹美元分隔符的 `P^c`、`M_{i+1}`、`x_i` 等上下标表达式。
- 保留普通文本、代码块和无公式上下文中的相似字符串，避免误判为数学公式。
- 扩充 KaTeX 渲染回归覆盖：行内/行间公式、重复转义、aligned、cases、matrix、equation、gathered、array、关系符号、表格、代码块、异常公式和安全转义。
- 增加翻译一致性防护：发送给 API/Agy 前锁定显式公式、裸公式行、上下标、关系运算符和数学符号；译文需通过 token 顺序、关系结构和数值序列校验，否则自动重试一次，仍失败则不显示且不写入缓存。
- 读取历史划词翻译缓存时重新执行一致性校验，自动丢弃旧的错译公式缓存，避免修复后继续展示错误结果。

### 验证

- `npm test`：132 项测试全部通过。
- 额外执行 1,000 组公式/普通文本回归压力测试，公式渲染和误判断言全部通过。
- 重新构建并打包 `zotero-academic-translator-1.0.50.xpi`，通过 JavaScript 语法检查和 ZIP 完整性检查。

### 兼容性

- 版本号升级到 `1.0.50`，Zotero 支持范围保持 `7.0` 至 `10.*`。
- 不改变现有配置格式和 API Key 存储方式。
